{% macro hero(title, subtitle, image, link_text, link_href, links, icons) %}
    <div class="aubskate-hero {% if not image %}aubskate-section-hero{% endif %}" {% if image %}style="background-image: url('{{ image }}')"{% endif %}>
        <div class="aubskate-hero-content">
            {% if title -%}
                <h2 class="aubskate-hero-title">{{ title }}</h2>
             {%- endif %}

             {% if subtitle -%}
                <div class="aubskate-subtitle">
                    {{ subtitle | markdown | safe }}
                </div>
            {%- endif %}


            {% if link_text -%}
                <a href="{{ link_href }}" class="aubskate-button">
                    {{ link_text }}
                    <img src="{{ icons.button_arrow }}" alt="" class="aubskate-button-icon" />
                </a>
            {%- endif %}
        </div>

        {% if links -%}
            <div class="aubskate-hero-contact">
                {% for link in links -%}
                    <div class="aubskate-hero-contact-content">
                        <img src="{{ link.image }}" alt="" class="aubskate-hero-contact-icon" />
                        <div class="aubskate-hero-contact-info">
                            {{ link.link_content | markdown | safe }}
                        </div>
                    </div>
                {%- endfor %}
            </div>
        {%- endif %}

    </div>
{% endmacro %}
