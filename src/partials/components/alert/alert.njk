{% macro alert(content, icons) %}
    <div class="aubskate-alert">
        <div class="aubskate-alert-icon-container">
            {% if content.icon -%}
                <img src="{{ content.icon }}" alt="" class="aubskate-alert-icon aubskate-alert-custom-icon" />
            {% else %}
                <img src="{{ icons.alert }}" alt="" class="aubskate-alert-icon aubskate-alert-standard-icon" />
            {%- endif %}
        </div>

        <div class="aubskate-alert-message">
            {% if content.title -%}
                <h2 class="aubskate-alert-title">{{ content.title }}</h2>
            {%- endif %}

            <div class="aubskate-alert-description">
                {{ content.description | markdown | safe }}
            </div>
        </div>
    </div>
{% endmacro %}
