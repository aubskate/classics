{% macro relatedLinks(resources, icons) %}
    <aside class="aubskate-sidebar">
        <h2 class="aubskate-sidebar-header">Top Resources</h2>
        <ul class="aubskate-sidebar-list">
            {% for resource in resources -%}
                <li class="aubskate-sidebar-list-item">
                    {% if resource.resource_link -%}
                        <a class="aubskate-sidebar-link" href="{{  resource.resource_link  }}">
                    {%- endif %}
                    {% if resource.resource_icon -%}
                        <img class="aubskate-sidebar-icon" src="{{  resource.resource_icon  }}" alt="" />
                    {% else %}
                        <img class="aubskate-sidebar-icon" inline src="{{ icons.document}}" alt="">
                    {%- endif %}

                    <span class="aubskate-sidebar-text">{{ resource.resource_name }}</span>

                    {% if resource.resource_link -%}
                        </a>
                    {%- endif %}
                </li>
            {%- endfor %}
        </ul>
    </aside>
{% endmacro %}
