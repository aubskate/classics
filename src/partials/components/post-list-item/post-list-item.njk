{% macro postListItem(links, icons) %}
    <ul class="aubskate-post-grid">
        {% for link in links -%}
            <li class="aubskate-post-item">
                <a class="aubskate-post-link" href="{{ link.slug }}">
                    {% if link.icon -%}
                        {{ link.icon | svgContents | safe }}
                    {% else %}
                        {% set icon = icons.post_link | string %}
                        {{ icon | svgContents | safe }}
                    {%- endif %}

                    <span class="aubskate-post-link-title">{{ link.title }}</span>
                </a>
            </li>
        {%- endfor %}
    </ul>
{% endmacro %}
